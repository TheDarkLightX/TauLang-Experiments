# Results

This directory is for benchmark reports and generated summaries that are safe to publish.

Do not commit:

- local build directories
- downloaded Tau source
- private machine paths
- credentials or API keys
- huge raw logs unless they are intentionally archived

Prefer compact, reproducible summaries with commands and commit hashes.
