diff --git a/src/CMakeLists.txt b/src/CMakeLists.txt
index 946b6710..009000d7 100644
--- a/src/CMakeLists.txt
+++ b/src/CMakeLists.txt
@@ -14,6 +14,7 @@ set(TAU_HEADERS
 	boolean_algebras/tau_ba.h
 	boolean_algebras/bv_ba.h
 	boolean_algebras/finite_table_ba.h
+	boolean_algebras/qns_candidate_ba.h
 	defs.h             # common macro and types definitions
 	logging.h          # logging and its macros
 	tau_tree.h         # tau tree, node, tree traverser, builders,
diff --git a/src/boolean_algebras/qns_candidate_ba.h b/src/boolean_algebras/qns_candidate_ba.h
new file mode 100644
index 00000000..a8623ef3
--- /dev/null
+++ b/src/boolean_algebras/qns_candidate_ba.h
@@ -0,0 +1,267 @@
+// To view the license please visit https://github.com/IDNI/tau-lang/blob/main/LICENSE.md
+
+#ifndef __IDNI__TAU__BOOLEAN_ALGEBRAS__QNS_CANDIDATE_BA_H__
+#define __IDNI__TAU__BOOLEAN_ALGEBRAS__QNS_CANDIDATE_BA_H__
+
+#include <algorithm>
+#include <cctype>
+#include <cstdint>
+#include <cstdlib>
+#include <optional>
+#include <ostream>
+#include <string>
+
+#include "defs.h"
+#include "splitter_types.h"
+#include "tau_tree.h"
+
+namespace idni::tau_lang {
+
+struct qns8_ba {
+	static constexpr uint8_t mask_all = 0xff;
+
+	uint8_t mask = 0;
+
+	qns8_ba() = default;
+	explicit qns8_ba(uint8_t m) : mask(m) {}
+
+	static qns8_ba zero() { return qns8_ba(0); }
+	static qns8_ba one() { return qns8_ba(mask_all); }
+
+	qns8_ba operator&(const qns8_ba& x) const { return qns8_ba(mask & x.mask); }
+	qns8_ba operator|(const qns8_ba& x) const { return qns8_ba(mask | x.mask); }
+	qns8_ba operator^(const qns8_ba& x) const { return qns8_ba(mask ^ x.mask); }
+	qns8_ba operator+(const qns8_ba& x) const { return *this ^ x; }
+	qns8_ba operator~() const { return qns8_ba(static_cast<uint8_t>(~mask)); }
+
+	auto operator<=>(const qns8_ba& x) const = default;
+
+	bool is_zero() const { return mask == 0; }
+	bool is_one() const { return mask == mask_all; }
+};
+
+struct qns64_ba {
+	static constexpr uint64_t mask_all = UINT64_MAX;
+
+	uint64_t mask = 0;
+
+	qns64_ba() = default;
+	explicit qns64_ba(uint64_t m) : mask(m) {}
+
+	static qns64_ba zero() { return qns64_ba(0); }
+	static qns64_ba one() { return qns64_ba(mask_all); }
+
+	qns64_ba operator&(const qns64_ba& x) const { return qns64_ba(mask & x.mask); }
+	qns64_ba operator|(const qns64_ba& x) const { return qns64_ba(mask | x.mask); }
+	qns64_ba operator^(const qns64_ba& x) const { return qns64_ba(mask ^ x.mask); }
+	qns64_ba operator+(const qns64_ba& x) const { return *this ^ x; }
+	qns64_ba operator~() const { return qns64_ba(~mask); }
+
+	auto operator<=>(const qns64_ba& x) const = default;
+
+	bool is_zero() const { return mask == 0; }
+	bool is_one() const { return mask == mask_all; }
+};
+
+inline bool qns8_env_flag_enabled() {
+	const char* flag = std::getenv("TAU_ENABLE_QNS_BA");
+	if (!flag) return false;
+	std::string value(flag);
+	std::ranges::transform(value, value.begin(),
+		[](unsigned char c) { return static_cast<char>(std::tolower(c)); });
+	return value != "0" && value != "false" && value != "no";
+}
+
+inline bool qns_ba_env_flag_enabled() {
+	return qns8_env_flag_enabled();
+}
+
+template<NodeType node>
+tref qns8_type() {
+	using tau = tree<node>;
+	tref type = tau::get(tau::type, "qns8");
+	return tau::get(tau::typed, type);
+}
+
+template<NodeType node>
+bool is_qns8_type(tref t) {
+	using tau = tree<node>;
+	return tau::get(t)[0].get_string() == "qns8";
+}
+
+template<NodeType node>
+tref qns64_type() {
+	using tau = tree<node>;
+	tref type = tau::get(tau::type, "qns64");
+	return tau::get(tau::typed, type);
+}
+
+template<NodeType node>
+bool is_qns64_type(tref t) {
+	using tau = tree<node>;
+	return tau::get(t)[0].get_string() == "qns64";
+}
+
+inline qns8_ba normalize(const qns8_ba& f) { return qns8_ba(f.mask); }
+inline bool is_syntactic_one(const qns8_ba& f) { return f.is_one(); }
+inline bool is_syntactic_zero(const qns8_ba& f) { return f.is_zero(); }
+inline bool is_closed(const qns8_ba&) { return true; }
+inline qns64_ba normalize(const qns64_ba& f) { return qns64_ba(f.mask); }
+inline bool is_syntactic_one(const qns64_ba& f) { return f.is_one(); }
+inline bool is_syntactic_zero(const qns64_ba& f) { return f.is_zero(); }
+inline bool is_closed(const qns64_ba&) { return true; }
+
+inline uint8_t qns8_least_atom(uint8_t mask) {
+	return static_cast<uint8_t>(mask & static_cast<uint8_t>(-mask));
+}
+
+inline qns8_ba splitter(const qns8_ba& f, splitter_type&) {
+	return qns8_ba(f.mask == 0 ? 0 : qns8_least_atom(f.mask));
+}
+
+inline uint64_t qns64_least_atom(uint64_t mask) {
+	return mask & (0 - mask);
+}
+
+inline qns64_ba splitter(const qns64_ba& f, splitter_type&) {
+	return qns64_ba(f.mask == 0 ? 0 : qns64_least_atom(f.mask));
+}
+
+inline bool operator==(const qns8_ba& lhs, const bool& rhs) {
+	return rhs ? lhs.is_one() : lhs.is_zero();
+}
+inline bool operator==(const bool& lhs, const qns8_ba& rhs) { return rhs == lhs; }
+inline bool operator!=(const qns8_ba& lhs, const bool& rhs) { return !(lhs == rhs); }
+inline bool operator!=(const bool& lhs, const qns8_ba& rhs) { return !(rhs == lhs); }
+inline bool operator==(const qns64_ba& lhs, const bool& rhs) {
+	return rhs ? lhs.is_one() : lhs.is_zero();
+}
+inline bool operator==(const bool& lhs, const qns64_ba& rhs) { return rhs == lhs; }
+inline bool operator!=(const qns64_ba& lhs, const bool& rhs) { return !(lhs == rhs); }
+inline bool operator!=(const bool& lhs, const qns64_ba& rhs) { return !(rhs == lhs); }
+
+inline std::ostream& operator<<(std::ostream& os, const qns8_ba& f) {
+	return os << static_cast<unsigned>(f.mask);
+}
+inline std::ostream& operator<<(std::ostream& os, const qns64_ba& f) {
+	return os << f.mask;
+}
+
+inline tref base_ba_symbol_simplification(tref symbol, const qns8_ba&) {
+	return symbol;
+}
+inline tref base_ba_term_simplification(tref term, const qns8_ba&) {
+	return term;
+}
+inline tref base_ba_symbol_simplification(tref symbol, const qns64_ba&) {
+	return symbol;
+}
+inline tref base_ba_term_simplification(tref term, const qns64_ba&) {
+	return term;
+}
+
+inline std::optional<uint8_t> parse_qns8_mask(std::string src) {
+	src.erase(std::remove_if(src.begin(), src.end(),
+		[](unsigned char c) { return std::isspace(c) || c == '{' || c == '}'; }),
+		src.end());
+	if (src.empty()) return {};
+
+	int base = 10;
+	if (src.size() > 2 && src[0] == '#' &&
+		(src[1] == 'x' || src[1] == 'X')) {
+		src = src.substr(2);
+		base = 16;
+	} else if (src.size() > 2 && src[0] == '0' &&
+		(src[1] == 'x' || src[1] == 'X')) {
+		src = src.substr(2);
+		base = 16;
+	}
+
+	size_t idx = 0;
+	try {
+		unsigned long value = std::stoul(src, &idx, base);
+		if (idx != src.size() || value > qns8_ba::mask_all) return {};
+		return static_cast<uint8_t>(value);
+	} catch (...) {
+		return {};
+	}
+}
+
+inline std::optional<uint64_t> parse_qns64_mask(std::string src) {
+	src.erase(std::remove_if(src.begin(), src.end(),
+		[](unsigned char c) { return std::isspace(c) || c == '{' || c == '}'; }),
+		src.end());
+	if (src.empty()) return {};
+
+	int base = 10;
+	if (src.size() > 2 && src[0] == '#' &&
+		(src[1] == 'x' || src[1] == 'X')) {
+		src = src.substr(2);
+		base = 16;
+	} else if (src.size() > 2 && src[0] == '0' &&
+		(src[1] == 'x' || src[1] == 'X')) {
+		src = src.substr(2);
+		base = 16;
+	}
+
+	size_t idx = 0;
+	try {
+		unsigned long long value = std::stoull(src, &idx, base);
+		if (idx != src.size()) return {};
+		return static_cast<uint64_t>(value);
+	} catch (...) {
+		return {};
+	}
+}
+
+template<typename... BAs>
+requires BAsPack<BAs...>
+std::optional<typename node<BAs...>::constant_with_type> parse_qns8(
+	const std::string& src)
+{
+	using node_t = node<BAs...>;
+	if (!qns_ba_env_flag_enabled()) {
+		LOG_ERROR << "qns8 constants require TAU_ENABLE_QNS_BA=1";
+		return {};
+	}
+	auto mask = parse_qns8_mask(src);
+	if (!mask) return {};
+	return typename node_t::constant_with_type{
+		std::variant<BAs...>{ qns8_ba(*mask) },
+		qns8_type<node_t>() };
+}
+
+template<typename... BAs>
+requires BAsPack<BAs...>
+std::optional<typename node<BAs...>::constant_with_type> parse_qns64(
+	const std::string& src)
+{
+	using node_t = node<BAs...>;
+	if (!qns_ba_env_flag_enabled()) {
+		LOG_ERROR << "qns64 constants require TAU_ENABLE_QNS_BA=1";
+		return {};
+	}
+	auto mask = parse_qns64_mask(src);
+	if (!mask) return {};
+	return typename node_t::constant_with_type{
+		std::variant<BAs...>{ qns64_ba(*mask) },
+		qns64_type<node_t>() };
+}
+
+} // namespace idni::tau_lang
+
+template<>
+struct std::hash<idni::tau_lang::qns8_ba> {
+	size_t operator()(const idni::tau_lang::qns8_ba& f) const noexcept {
+		return static_cast<size_t>(f.mask);
+	}
+};
+
+template<>
+struct std::hash<idni::tau_lang::qns64_ba> {
+	size_t operator()(const idni::tau_lang::qns64_ba& f) const noexcept {
+		return static_cast<size_t>(f.mask);
+	}
+};
+
+#endif // __IDNI__TAU__BOOLEAN_ALGEBRAS__QNS_CANDIDATE_BA_H__
diff --git a/src/main.cpp b/src/main.cpp
index 7741d294..72f693ed 100644
--- a/src/main.cpp
+++ b/src/main.cpp
@@ -9,6 +9,7 @@
 // including nso_ba, sbf_ba and interpreter directly instead of
 // #include "tau.h" to avoid error lines pointing to a generated tau.h
 #include "boolean_algebras/bv_ba.h"
+#include "boolean_algebras/qns_candidate_ba.h"
 #include "boolean_algebras/sbf_ba.h"
 #include "boolean_algebras/nso_ba.h"
 #include "boolean_algebras/tau_ba.h"
@@ -25,8 +26,8 @@ using namespace std;
 using namespace idni;
 using namespace idni::tau_lang;
 
-using runtime_node = tau_lang::node<tau_ba<bv, sbf_ba, ft4_ba>, bv, sbf_ba, ft4_ba>;
-using runtime_repl = repl_evaluator<bv, sbf_ba, ft4_ba>;
+using runtime_node = tau_lang::node<tau_ba<bv, sbf_ba, ft4_ba, qns8_ba, qns64_ba>, bv, sbf_ba, ft4_ba, qns8_ba, qns64_ba>;
+using runtime_repl = repl_evaluator<bv, sbf_ba, ft4_ba, qns8_ba, qns64_ba>;
 
 cli::options tau_options() {
 	cli::options opts;
diff --git a/src/runtime.h b/src/runtime.h
index fd5ad8b3..3b52ee7f 100644
--- a/src/runtime.h
+++ b/src/runtime.h
@@ -8,6 +8,7 @@
 #include "boolean_algebras/tau_ba.h"
 #include "boolean_algebras/bv_ba.h"
 #include "boolean_algebras/finite_table_ba.h"
+#include "boolean_algebras/qns_candidate_ba.h"
 #include "nso_rr.h"
 
 namespace idni::tau_lang {
@@ -57,25 +58,25 @@ struct nso_factory<tau_ba<bv, sbf_ba>, bv, sbf_ba> {
 };
 
 /**
- * @brief NSO factory used by the CLI with the feature-gated ft4 finite table BA.
+ * @brief NSO factory used by the CLI with feature-gated experiment BAs.
  */
 template<>
-struct nso_factory<tau_ba<bv, sbf_ba, ft4_ba>, bv, sbf_ba, ft4_ba> {
+struct nso_factory<tau_ba<bv, sbf_ba, ft4_ba, qns8_ba, qns64_ba>, bv, sbf_ba, ft4_ba, qns8_ba, qns64_ba> {
 	static std::vector<std::string> types();
 
 	static tref default_type();
 
-	static std::string one(tref type_tree = tau_type<node<tau_ba<bv, sbf_ba, ft4_ba>, bv, sbf_ba, ft4_ba>>());
+	static std::string one(tref type_tree = tau_type<node<tau_ba<bv, sbf_ba, ft4_ba, qns8_ba, qns64_ba>, bv, sbf_ba, ft4_ba, qns8_ba, qns64_ba>>());
 
-	static std::string zero(tref type_tree = tau_type<node<tau_ba<bv, sbf_ba, ft4_ba>, bv, sbf_ba, ft4_ba>>());
+	static std::string zero(tref type_tree = tau_type<node<tau_ba<bv, sbf_ba, ft4_ba, qns8_ba, qns64_ba>, bv, sbf_ba, ft4_ba, qns8_ba, qns64_ba>>());
 
 	static tref splitter_one(tref type_tree);
 
-	static tref unpack_tau_ba(const std::variant<tau_ba<bv, sbf_ba, ft4_ba>, bv, sbf_ba, ft4_ba>& v);
+	static tref unpack_tau_ba(const std::variant<tau_ba<bv, sbf_ba, ft4_ba, qns8_ba, qns64_ba>, bv, sbf_ba, ft4_ba, qns8_ba, qns64_ba>& v);
 
-	static std::variant<tau_ba<bv, sbf_ba, ft4_ba>, bv, sbf_ba, ft4_ba> pack_tau_ba(tref c);
+	static std::variant<tau_ba<bv, sbf_ba, ft4_ba, qns8_ba, qns64_ba>, bv, sbf_ba, ft4_ba, qns8_ba, qns64_ba> pack_tau_ba(tref c);
 
-	static std::variant<tau_ba<bv, sbf_ba, ft4_ba>, bv, sbf_ba, ft4_ba> to_base_ba_type(tref type_tree);
+	static std::variant<tau_ba<bv, sbf_ba, ft4_ba, qns8_ba, qns64_ba>, bv, sbf_ba, ft4_ba, qns8_ba, qns64_ba> to_base_ba_type(tref type_tree);
 
 };
 
@@ -102,17 +103,21 @@ inline std::optional<typename ba_constants<node<tau_ba<bv, sbf_ba>, bv, sbf_ba>>
 }
 
 template <>
-inline std::optional<typename ba_constants<node<tau_ba<bv, sbf_ba, ft4_ba>, bv, sbf_ba, ft4_ba>>::constant_with_type> ba_constants<node<tau_ba<bv, sbf_ba, ft4_ba>, bv, sbf_ba, ft4_ba>>::get(
+inline std::optional<typename ba_constants<node<tau_ba<bv, sbf_ba, ft4_ba, qns8_ba, qns64_ba>, bv, sbf_ba, ft4_ba, qns8_ba, qns64_ba>>::constant_with_type> ba_constants<node<tau_ba<bv, sbf_ba, ft4_ba, qns8_ba, qns64_ba>, bv, sbf_ba, ft4_ba, qns8_ba, qns64_ba>>::get(
 		const std::string& constant_source,
 		tref type_tree,
 		[[maybe_unused]] const std::string options) {
-	if (is_sbf_type<node<tau_ba<bv, sbf_ba, ft4_ba>, bv, sbf_ba, ft4_ba>>(type_tree))
-		return parse_sbf<tau_ba<bv, sbf_ba, ft4_ba>, bv, sbf_ba, ft4_ba>(constant_source);
-	if (is_bv_type_family<node<tau_ba<bv, sbf_ba, ft4_ba>, bv, sbf_ba, ft4_ba>>(type_tree))
-		return parse_bv<tau_ba<bv, sbf_ba, ft4_ba>, bv, sbf_ba, ft4_ba>(constant_source, type_tree);
-	if (is_ft4_type<node<tau_ba<bv, sbf_ba, ft4_ba>, bv, sbf_ba, ft4_ba>>(type_tree))
-		return parse_ft4<tau_ba<bv, sbf_ba, ft4_ba>, bv, sbf_ba, ft4_ba>(constant_source);
-	return parse_tau<bv, sbf_ba, ft4_ba>(constant_source);
+	if (is_sbf_type<node<tau_ba<bv, sbf_ba, ft4_ba, qns8_ba, qns64_ba>, bv, sbf_ba, ft4_ba, qns8_ba, qns64_ba>>(type_tree))
+		return parse_sbf<tau_ba<bv, sbf_ba, ft4_ba, qns8_ba, qns64_ba>, bv, sbf_ba, ft4_ba, qns8_ba, qns64_ba>(constant_source);
+	if (is_bv_type_family<node<tau_ba<bv, sbf_ba, ft4_ba, qns8_ba, qns64_ba>, bv, sbf_ba, ft4_ba, qns8_ba, qns64_ba>>(type_tree))
+		return parse_bv<tau_ba<bv, sbf_ba, ft4_ba, qns8_ba, qns64_ba>, bv, sbf_ba, ft4_ba, qns8_ba, qns64_ba>(constant_source, type_tree);
+	if (is_ft4_type<node<tau_ba<bv, sbf_ba, ft4_ba, qns8_ba, qns64_ba>, bv, sbf_ba, ft4_ba, qns8_ba, qns64_ba>>(type_tree))
+		return parse_ft4<tau_ba<bv, sbf_ba, ft4_ba, qns8_ba, qns64_ba>, bv, sbf_ba, ft4_ba, qns8_ba, qns64_ba>(constant_source);
+	if (is_qns8_type<node<tau_ba<bv, sbf_ba, ft4_ba, qns8_ba, qns64_ba>, bv, sbf_ba, ft4_ba, qns8_ba, qns64_ba>>(type_tree))
+		return parse_qns8<tau_ba<bv, sbf_ba, ft4_ba, qns8_ba, qns64_ba>, bv, sbf_ba, ft4_ba, qns8_ba, qns64_ba>(constant_source);
+	if (is_qns64_type<node<tau_ba<bv, sbf_ba, ft4_ba, qns8_ba, qns64_ba>, bv, sbf_ba, ft4_ba, qns8_ba, qns64_ba>>(type_tree))
+		return parse_qns64<tau_ba<bv, sbf_ba, ft4_ba, qns8_ba, qns64_ba>, bv, sbf_ba, ft4_ba, qns8_ba, qns64_ba>(constant_source);
+	return parse_tau<bv, sbf_ba, ft4_ba, qns8_ba, qns64_ba>(constant_source);
 }
 
 } // namespace idni::tau_lang
diff --git a/src/runtime.tmpl.h b/src/runtime.tmpl.h
index e08cab14..976194b3 100644
--- a/src/runtime.tmpl.h
+++ b/src/runtime.tmpl.h
@@ -123,20 +123,20 @@ inline std::variant<tau_ba<bv, sbf_ba>, bv, sbf_ba> nso_factory<tau_ba<bv, sbf_b
 	}
 }
 
-inline std::vector<std::string> nso_factory<tau_ba<bv, sbf_ba, ft4_ba>,
-	bv, sbf_ba, ft4_ba>::types() {
-	return { "sbf", "tau", "bv", "ft4" };
+inline std::vector<std::string> nso_factory<tau_ba<bv, sbf_ba, ft4_ba, qns8_ba, qns64_ba>,
+	bv, sbf_ba, ft4_ba, qns8_ba, qns64_ba>::types() {
+	return { "sbf", "tau", "bv", "ft4", "qns8", "qns64" };
 }
 
-inline tref nso_factory<tau_ba<bv, sbf_ba, ft4_ba>,
-	bv, sbf_ba, ft4_ba>::default_type() {
-	return tau_type<node<tau_ba<bv, sbf_ba, ft4_ba>, bv, sbf_ba, ft4_ba>>();
+inline tref nso_factory<tau_ba<bv, sbf_ba, ft4_ba, qns8_ba, qns64_ba>,
+	bv, sbf_ba, ft4_ba, qns8_ba, qns64_ba>::default_type() {
+	return tau_type<node<tau_ba<bv, sbf_ba, ft4_ba, qns8_ba, qns64_ba>, bv, sbf_ba, ft4_ba, qns8_ba, qns64_ba>>();
 }
 
-inline std::string nso_factory<tau_ba<bv, sbf_ba, ft4_ba>,
-	bv, sbf_ba, ft4_ba>::one(tref type_tree)
+inline std::string nso_factory<tau_ba<bv, sbf_ba, ft4_ba, qns8_ba, qns64_ba>,
+	bv, sbf_ba, ft4_ba, qns8_ba, qns64_ba>::one(tref type_tree)
 {
-	using node_t = node<tau_ba<bv, sbf_ba, ft4_ba>, bv, sbf_ba, ft4_ba>;
+	using node_t = node<tau_ba<bv, sbf_ba, ft4_ba, qns8_ba, qns64_ba>, bv, sbf_ba, ft4_ba, qns8_ba, qns64_ba>;
 	if (is_sbf_type<node_t>(type_tree)) {
 		return "1";
 	} else if (is_bv_type_family<node_t>(type_tree)) {
@@ -144,13 +144,17 @@ inline std::string nso_factory<tau_ba<bv, sbf_ba, ft4_ba>,
 			).getBitVectorValue(10);
 	} else if (is_ft4_type<node_t>(type_tree)) {
 		return "15";
+	} else if (is_qns8_type<node_t>(type_tree)) {
+		return "255";
+	} else if (is_qns64_type<node_t>(type_tree)) {
+		return "18446744073709551615";
 	} else return "T";
 }
 
-inline std::string nso_factory<tau_ba<bv, sbf_ba, ft4_ba>,
-	bv, sbf_ba, ft4_ba>::zero(tref type_tree)
+inline std::string nso_factory<tau_ba<bv, sbf_ba, ft4_ba, qns8_ba, qns64_ba>,
+	bv, sbf_ba, ft4_ba, qns8_ba, qns64_ba>::zero(tref type_tree)
 {
-	using node_t = node<tau_ba<bv, sbf_ba, ft4_ba>, bv, sbf_ba, ft4_ba>;
+	using node_t = node<tau_ba<bv, sbf_ba, ft4_ba, qns8_ba, qns64_ba>, bv, sbf_ba, ft4_ba, qns8_ba, qns64_ba>;
 	if (is_sbf_type<node_t>(type_tree)) {
 		return "0";
 	} else if (is_bv_type_family<node_t>(type_tree)) {
@@ -158,19 +162,23 @@ inline std::string nso_factory<tau_ba<bv, sbf_ba, ft4_ba>,
 			).getBitVectorValue(10);
 	} else if (is_ft4_type<node_t>(type_tree)) {
 		return "0";
+	} else if (is_qns8_type<node_t>(type_tree)) {
+		return "0";
+	} else if (is_qns64_type<node_t>(type_tree)) {
+		return "0";
 	} else return "F";
 }
 
-inline tref nso_factory<tau_ba<bv, sbf_ba, ft4_ba>,
-	bv, sbf_ba, ft4_ba>::splitter_one(tref type_tree)
+inline tref nso_factory<tau_ba<bv, sbf_ba, ft4_ba, qns8_ba, qns64_ba>,
+	bv, sbf_ba, ft4_ba, qns8_ba, qns64_ba>::splitter_one(tref type_tree)
 {
-	using node_t = node<tau_ba<bv, sbf_ba, ft4_ba>, bv, sbf_ba, ft4_ba>;
+	using node_t = node<tau_ba<bv, sbf_ba, ft4_ba, qns8_ba, qns64_ba>, bv, sbf_ba, ft4_ba, qns8_ba, qns64_ba>;
 	using tau = tree<node_t>;
 	DBG(assert(!is_bv_type_family<node_t>(type_tree)));
 	if (is_sbf_type<node_t>(type_tree)) {
 		return tau::get(tau::bf,
 			tau::get_ba_constant(
-				sbf_ba_factory<tau_ba<bv, sbf_ba, ft4_ba>, bv, sbf_ba, ft4_ba>::splitter_one(),
+				sbf_ba_factory<tau_ba<bv, sbf_ba, ft4_ba, qns8_ba, qns64_ba>, bv, sbf_ba, ft4_ba, qns8_ba, qns64_ba>::splitter_one(),
 				sbf_type<node_t>()));
 	}
 	if (is_ft4_type<node_t>(type_tree)) {
@@ -179,38 +187,56 @@ inline tref nso_factory<tau_ba<bv, sbf_ba, ft4_ba>,
 				typename node_t::constant{ ft4_ba(1) },
 				ft4_type<node_t>()));
 	}
+	if (is_qns8_type<node_t>(type_tree)) {
+		splitter_type st;
+		return tau::get(tau::bf,
+			tau::get_ba_constant(
+				splitter(qns8_ba::one(), st),
+				qns8_type<node_t>()));
+	}
+	if (is_qns64_type<node_t>(type_tree)) {
+		splitter_type st;
+		return tau::get(tau::bf,
+			tau::get_ba_constant(
+				splitter(qns64_ba::one(), st),
+				qns64_type<node_t>()));
+	}
 	return tau::get(tau::bf,
 		tau::get_ba_constant(
-			tau_ba_factory<bv, sbf_ba, ft4_ba>::splitter_one(),
+			tau_ba_factory<bv, sbf_ba, ft4_ba, qns8_ba, qns64_ba>::splitter_one(),
 			tau_type<node_t>()));
 }
 
-inline tref nso_factory<tau_ba<bv, sbf_ba, ft4_ba>,
-	bv, sbf_ba, ft4_ba>::unpack_tau_ba(
-		const std::variant<tau_ba<bv, sbf_ba, ft4_ba>, bv, sbf_ba, ft4_ba>& v)
+inline tref nso_factory<tau_ba<bv, sbf_ba, ft4_ba, qns8_ba, qns64_ba>,
+	bv, sbf_ba, ft4_ba, qns8_ba, qns64_ba>::unpack_tau_ba(
+		const std::variant<tau_ba<bv, sbf_ba, ft4_ba, qns8_ba, qns64_ba>, bv, sbf_ba, ft4_ba, qns8_ba, qns64_ba>& v)
 {
-	if (!std::holds_alternative<tau_ba<bv, sbf_ba, ft4_ba>>(v)) return {};
-	const auto unpacked = std::get<tau_ba<bv, sbf_ba, ft4_ba>>(v);
+	if (!std::holds_alternative<tau_ba<bv, sbf_ba, ft4_ba, qns8_ba, qns64_ba>>(v)) return {};
+	const auto unpacked = std::get<tau_ba<bv, sbf_ba, ft4_ba, qns8_ba, qns64_ba>>(v);
 	return unpacked.nso_rr.main->get();
 }
 
-inline std::variant<tau_ba<bv, sbf_ba, ft4_ba>, bv, sbf_ba, ft4_ba>
-	nso_factory<tau_ba<bv, sbf_ba, ft4_ba>,
-	bv, sbf_ba, ft4_ba>::pack_tau_ba(tref c) {
-	tau_ba<bv, sbf_ba, ft4_ba> t {c};
+inline std::variant<tau_ba<bv, sbf_ba, ft4_ba, qns8_ba, qns64_ba>, bv, sbf_ba, ft4_ba, qns8_ba, qns64_ba>
+	nso_factory<tau_ba<bv, sbf_ba, ft4_ba, qns8_ba, qns64_ba>,
+	bv, sbf_ba, ft4_ba, qns8_ba, qns64_ba>::pack_tau_ba(tref c) {
+	tau_ba<bv, sbf_ba, ft4_ba, qns8_ba, qns64_ba> t {c};
 	return {t};
 }
 
-inline std::variant<tau_ba<bv, sbf_ba, ft4_ba>, bv, sbf_ba, ft4_ba>
-	nso_factory<tau_ba<bv, sbf_ba, ft4_ba>,
-	bv, sbf_ba, ft4_ba>::to_base_ba_type(tref type_tree) {
-	using node_t = node<tau_ba<bv, sbf_ba, ft4_ba>, bv, sbf_ba, ft4_ba>;
+inline std::variant<tau_ba<bv, sbf_ba, ft4_ba, qns8_ba, qns64_ba>, bv, sbf_ba, ft4_ba, qns8_ba, qns64_ba>
+	nso_factory<tau_ba<bv, sbf_ba, ft4_ba, qns8_ba, qns64_ba>,
+	bv, sbf_ba, ft4_ba, qns8_ba, qns64_ba>::to_base_ba_type(tref type_tree) {
+	using node_t = node<tau_ba<bv, sbf_ba, ft4_ba, qns8_ba, qns64_ba>, bv, sbf_ba, ft4_ba, qns8_ba, qns64_ba>;
 	if (is_tau_type<node_t>(type_tree)) {
-		return { tau_ba<bv, sbf_ba, ft4_ba>() };
+		return { tau_ba<bv, sbf_ba, ft4_ba, qns8_ba, qns64_ba>() };
 	} else if (is_sbf_type<node_t>(type_tree)) {
 		return { sbf_ba() };
 	} else if (is_ft4_type<node_t>(type_tree)) {
 		return { ft4_ba() };
+	} else if (is_qns8_type<node_t>(type_tree)) {
+		return { qns8_ba() };
+	} else if (is_qns64_type<node_t>(type_tree)) {
+		return { qns64_ba() };
 	} else {
 		DBG(assert(is_bv_type_family<node_t>(type_tree)));
 		return { bv() };
